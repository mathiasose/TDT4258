# Add files and commands to this file, like the example:
#   watch(%r{file/path}) { `command(s)` }
#

guard 'shell' do
  watch(%r{(.+)\.tex}) do |m|
    `pdflatex -interaction=batchmode group_13_exercise_2_report.tex >/dev/null`
  end
end
